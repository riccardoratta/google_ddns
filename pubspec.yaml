name: dns_updater
description: A simple utility to update Google Domains DNS to the current public IP.
version: 1.0.0

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies: 
  args: ^2.1.1
  http: ^0.13.1
  logging: ^1.0.1
